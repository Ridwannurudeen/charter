# Launch checklist

- [x] Confirm Sepolia contracts are deployed and verified.
- [x] Commit `deployments/sepolia/` artifacts.
- [x] Fill the README Sepolia addresses table.
- [x] Replace `docs/E2E-RUN.md` with the fresh round-two redeploy and e2e tx log.
- [x] Confirm `web/.env.local` is generated from `npm run export:addresses`.
- [x] Rebuild the web app and confirm the deployment-preview banner is gone.
- [x] Remove `HANDOFF.md`, `HANDOFF-2.md`, `AGENTS.md`, `web/CLAUDE.md`, and `web/AGENTS.md` from the repo before it
      goes public.
- [x] Make the GitHub repo public.
- [x] Add the landing-page GitHub link if the repo is public.
- [x] Add the final live site URL to `docs/X-THREAD.md`.
- [x] Add the demo video URL to `docs/X-THREAD.md`.
- [ ] Paste the final X thread URL into the submission form.
