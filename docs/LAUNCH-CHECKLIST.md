# Launch checklist

- [ ] Confirm Sepolia contracts are deployed and verified.
- [ ] Commit `deployments/sepolia/` artifacts.
- [ ] Fill the README Sepolia addresses table.
- [ ] Confirm `web/.env.local` is generated from `npm run export:addresses`.
- [ ] Rebuild the web app and confirm the deployment-preview banner is gone.
- [ ] Remove `HANDOFF.md`, `AGENTS.md`, `web/CLAUDE.md`, and `web/AGENTS.md` from the repo before it goes public.
- [ ] Make the GitHub repo public.
- [ ] Add the landing-page GitHub link if the repo is public.
- [ ] Add the final live site URL to `docs/X-THREAD.md`.
- [ ] Add the demo video URL to `docs/X-THREAD.md`.
- [ ] Paste the final X thread URL into the submission form.
